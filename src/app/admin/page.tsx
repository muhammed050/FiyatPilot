import { createClient } from '@/lib/supabase/server';
import { redirect } from 'next/navigation';
import { products } from '@/lib/products';
export default async function Admin(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/giris?next=/admin');const {data:profile}=await supabase.from('profiles').select('role').eq('id',user.id).single();if(!profile||!['admin','super_admin','editor','seo_manager','analyst'].includes(profile.role))redirect('/');return <main className="min-h-screen bg-slate-950 text-white"><div className="mx-auto grid max-w-[1440px] md:grid-cols-[240px_1fr]"><aside className="hidden border-r border-white/10 p-5 md:block"><div className="text-xl font-black">Fiyat<span className="text-blue-400">Pilot</span></div><div className="mt-8 grid gap-1 text-sm text-slate-300">{['Dashboard','Ürünler','Kategoriler','Mağazalar','Affiliate','Fiyatlar','Fiyat Alarmları','SEO','Anahtar Kelimeler','AI İçerik','Search Console','Analytics','Gelir','Kullanıcılar','Otomasyon','Ayarlar'].map(x=><div className="rounded-lg px-3 py-2 hover:bg-white/5" key={x}>{x}</div>)}</div></aside><section className="p-5 md:p-8"><div className="flex items-center justify-between"><div><div className="text-sm text-blue-400">FiyatPilot Admin · {profile.role}</div><h1 className="mt-1 text-3xl font-black">Kontrol Merkezi</h1></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Organic Traffic','—'],['Affiliate Clicks','—'],['Revenue','₺ —'],['Indexed Pages','—']].map(([a,b])=><div className="rounded-2xl border border-white/10 bg-white/[.04] p-5" key={a}><div className="text-sm text-slate-400">{a}</div><div className="mt-2 text-3xl font-black">{b}</div><div className="mt-2 text-xs text-slate-500">Canlı entegrasyon bekleniyor</div></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-2"><div className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><h2 className="font-bold">SEO Fırsatları</h2><div className="mt-4 rounded-xl border border-white/10 p-4"><div className="font-semibold">Search Console bağlantısı</div><p className="mt-1 text-sm text-slate-400">Bağlandığında 5–20 pozisyon fırsatları, düşük CTR ve büyüyen sorgular otomatik analiz edilir.</p></div></div><div className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><h2 className="font-bold">AI Copilot</h2><p className="mt-2 text-sm text-slate-400">SEO, ürün ve gelir verilerini analiz eden yönetim asistanı.</p><div className="mt-4 flex gap-2"><input className="min-w-0 flex-1 rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none" placeholder="En büyük SEO fırsatlarını bul..."/><button className="rounded-xl bg-blue-600 px-4 font-bold">Sor</button></div></div></div><div className="mt-6 overflow-x-auto rounded-2xl border border-white/10 bg-white/[.04]"><table className="w-full min-w-[720px] text-left text-sm"><thead><tr className="border-b border-white/10 text-slate-400"><th className="p-4">Ürün</th><th>Fiyat</th><th>AI</th><th>Durum</th></tr></thead><tbody>{products.map(p=><tr className="border-b border-white/5" key={p.id}><td className="p-4 font-semibold">{p.name}</td><td>{p.price.toLocaleString('tr-TR')} TL</td><td>{p.aiScore}</td><td><span className="rounded-full bg-amber-400/10 px-2 py-1 text-xs text-amber-300">Seed veri</span></td></tr>)}</tbody></table></div></section></div></main>}
