import { products } from '@/lib/products';
export default function ComparePage(){return <main className="container py-10"><h1 className="text-3xl font-black">Ürünleri Karşılaştır</h1><p className="mt-2 text-slate-500">En fazla 4 ürünü teknik özellik, fiyat ve FiyatPilot skorlarıyla karşılaştır.</p><div className="mt-8 overflow-x-auto surface"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr className="border-b bg-slate-50"><th className="p-4">Özellik</th>{products.slice(0,3).map(p=><th className="p-4" key={p.id}>{p.name}</th>)}</tr></thead><tbody>{[['Güncel fiyat',...products.slice(0,3).map(p=>p.price.toLocaleString('tr-TR')+' TL')],['AI Skoru',...products.slice(0,3).map(p=>p.aiScore+'/100')],['Fiyat/Performans',...products.slice(0,3).map(p=>p.valueScore+'/100')],['Puan',...products.slice(0,3).map(p=>p.rating+'/5')],['RAM',...products.slice(0,3).map(p=>p.specs.RAM||'—')]].map((r,i)=><tr className="border-b last:border-0" key={i}>{r.map((x,j)=><td className={'p-4 '+(j===0?'font-bold':'')} key={j}>{x}</td>)}</tr>)}</tbody></table></div><section className="mt-8 surface border-violet-100 bg-violet-50 p-6"><div className="text-sm font-bold text-violet-700">✨ FiyatPilot Önerisi</div><h2 className="mt-1 text-xl font-black">Fiyat/performans için Lenovo LOQ öne çıkıyor.</h2><p className="mt-2 text-sm text-slate-600">Karar motoru fiyat, kalite ve değer skorlarını birlikte değerlendirerek en dengeli seçeneği öne çıkarır.</p></section></main>}
