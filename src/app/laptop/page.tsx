import { products } from '@/lib/products';
export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=slug==='laptop'?'En İyi Laptoplar':slug.replaceAll('-',' ');const items=products.filter(p=>p.category.toLowerCase()===slug.replaceAll('-',' '));return <main className="container py-10"><div className="max-w-3xl"><div className="text-sm font-bold text-blue-600">FiyatPilot kategori</div><h1 className="mt-2 text-4xl font-black capitalize">{title}</h1><p className="mt-3 leading-7 text-slate-600">Güncel fiyatlar, teknik özellikler ve FiyatPilot puanlarıyla doğru ürünü daha hızlı seç. Filtreleri ve karşılaştırmayı kullanarak bütçene uygun seçenekleri daralt.</p></div><div className="mt-8 flex gap-3 overflow-x-auto pb-2"><button className="btn btn-secondary whitespace-nowrap">Marka</button><button className="btn btn-secondary whitespace-nowrap">Fiyat</button><button className="btn btn-secondary whitespace-nowrap">Fiyat/Performans</button><button className="btn btn-secondary whitespace-nowrap">En yüksek puan</button></div><div className="mt-6 grid-auto">{(items.length?items:products).map(p=><article className="surface overflow-hidden" key={p.id}><img src={p.image} alt={p.name} className="aspect-[4/3] w-full object-cover"/><div className="p-4"><div className="text-xs font-bold text-slate-500">{p.brand}</div><h2 className="mt-1 font-bold">{p.name}</h2><div className="mt-3 flex justify-between"><span className="price text-xl font-black">{p.price.toLocaleString('tr-TR')} TL</span><span className="rounded-full bg-violet-50 px-2 py-1 text-xs font-bold text-violet-700">AI {p.aiScore}</span></div><a className="btn btn-primary mt-4 w-full" href={'/urun/'+p.slug}>Ürünü İncele</a></div></article>)}</div></main>}
