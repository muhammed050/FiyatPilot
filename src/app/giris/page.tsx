import { createClient } from '@/lib/supabase/server';
export default function Login(){async function signIn(formData:FormData){'use server';const supabase=await createClient();const email=String(formData.get('email')||'');const password=String(formData.get('password')||'');await supabase.auth.signInWithPassword({email,password});}return <main className="container flex min-h-[70vh] items-center justify-center py-10"><div className="surface w-full max-w-md p-7"><h1 className="text-2xl font-black">FiyatPilot’a giriş yap</h1><p className="mt-2 text-sm text-slate-500">Favorilerini, karşılaştırmalarını ve fiyat alarmlarını senkronize et.</p><form action={signIn} className="mt-6 grid gap-3"><input name="email" type="email" required placeholder="E-posta" className="rounded-xl border p-3"/><input name="password" type="password" required placeholder="Şifre" className="rounded-xl border p-3"/><button className="btn btn-primary">Giriş Yap</button></form><p className="mt-5 text-xs text-slate-500">Google OAuth ve kayıt akışı Supabase Auth yapılandırmasıyla eklenebilir.</p></div></main>}
